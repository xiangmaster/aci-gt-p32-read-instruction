name: Ground Truth P32-E02
on:
  pull_request_target:
    types: [opened, synchronize]
permissions: read-all
jobs:
  fixture:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - run: cat CLAUDE.md > agent-context.txt
